// Hàm này dùng để claim token, các tham số truyền vào:
+> _allocationType: phân loại allocation (preseed = 0, preseed = 1, private, ...)
+> _totalVestingAmount: tổng số token mà users sẽ được nhận
+> _TGE: Phần trăm TGE
+> _startTime: thời gian bắt đầu vesting
+> _cliffDuration: quãng thời gian dừng trả token
+> _vestingDuration: quãng thời gian vesting
- function claimToken(uint _allocationType, uint256 _totalVestingAmount, uint128 _TGE, uint128 _startTime, uint128 _cliffDuration, uint128 _vestingDuration)
// Hàm này dùng để tính toán số lượng vesting của mỗi chu kỳ, các tham số truyền vào:
+> _user: địa chỉ của user
+> _allocationType: phân loại allocation (preseed = 0, seed = 1, private, ...)
- claimableToken(address _user, uint _allocationType)
// Hàm này dùng để kiểm tra proof của merkle tree, các tham số truyền vào:
+> _allocationType: phân loại allocation (preseed = 0, preseed = 1, private, ...)
+> _totalVestingAmount: tổng số token mà users sẽ được nhận
+> _TGE: Phần trăm TGE
+> _startTime: thời gian bắt đầu vesting
+> _cliffDuration: quãng thời gian dừng trả token
+> _vestingDuration: quãng thời gian vesting
- function _verifyVestingUser(uint _allocationType, uint256 _totalVestingAmount, uint128 _TGE, uint128 _startTime, uint128 _cliffDuration, uint128 _vestingDuration)
// Hàm này dùng để update root của merkle tree, tham số truyền vào:
+> _root: root của merkle tree
- function updateRoot(bytes32 _root)
// Hàm này dùng để add Admin cho contract, tham số truyền vào:
+> _AddAdmin: địa chỉ address
- addAdmin(address _AddAdmin)
// Hàm này dùng để remove Admin cho contract, tham số truyền vào:
+> _RemoveAdmin: địa chỉ address
- removeAdmin(address _RemoveAdmin)


